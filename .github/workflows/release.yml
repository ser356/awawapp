name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  build:
    permissions:
      contents: write

    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: ubuntu-22.04
            artifact-name: awawapp-linux
          - platform: macos-14
            artifact-name: awawapp-macos-arm64
          - platform: macos-13
            artifact-name: awawapp-macos-x64
          - platform: windows-latest
            artifact-name: awawapp-windows

    runs-on: ${{ matrix.platform }}

    steps:
      - uses: actions/checkout@v4

      - name: Install Linux dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf \
            libssl-dev \
            pkg-config \
            mpv

      - name: Install macOS dependencies
        if: startsWith(matrix.platform, 'macos')
        run: brew install mpv

      # Windows: mpv is auto-downloaded by bundle-mpv.sh if not in PATH

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri -> target

      - name: Install frontend dependencies
        run: npm ci

      - name: Build CLI sidecar
        shell: bash
        run: bash scripts/build-cli.sh

      - name: Bundle mpv + dylibs + uosc
        shell: bash
        run: bash scripts/bundle-mpv.sh

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: "awawapp ${{ github.ref_name }}"
          releaseBody: |
            See the assets below to download for your platform.
          releaseDraft: true
          prerelease: false

      - name: Repackage DMG with quarantine fix and README
        if: startsWith(matrix.platform, 'macos')
        run: bash scripts/package-dmg.sh

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact-name }}
          path: |
            src-tauri/target/release/bundle/deb/*.deb
            src-tauri/target/release/bundle/rpm/*.rpm
            src-tauri/target/release/bundle/appimage/*.AppImage
            src-tauri/target/release/bundle/dmg/*.dmg
            src-tauri/target/release/bundle/macos/*.app.tar.gz
            src-tauri/target/release/bundle/nsis/*.exe
          if-no-files-found: ignore
          retention-days: 7
