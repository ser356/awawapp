name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  build:
    permissions:
      contents: write

    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: ubuntu-22.04
            artifact-name: awawapp-linux
          - platform: macos-latest
            artifact-name: awawapp-macos
          - platform: windows-latest
            artifact-name: awawapp-windows

    runs-on: ${{ matrix.platform }}

    steps:
      - uses: actions/checkout@v4

      - name: Install Linux dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf \
            libssl-dev \
            pkg-config

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri -> target

      - name: Install frontend dependencies
        run: npm ci

      - name: Build sidecar binary
        shell: bash
        run: |
          TARGET_TRIPLE=$(rustc -vV | sed -n 's|host: ||p')
          mkdir -p src-tauri/binaries
          # Create placeholder so tauri-build doesn't fail during cargo build
          if [[ "$RUNNER_OS" == "Windows" ]]; then
            touch "src-tauri/binaries/awaw-${TARGET_TRIPLE}.exe"
          else
            touch "src-tauri/binaries/awaw-${TARGET_TRIPLE}"
          fi
          cargo build --release --bin awaw --manifest-path src-tauri/Cargo.toml
          # Replace placeholder with the real binary
          if [[ "$RUNNER_OS" == "Windows" ]]; then
            cp "src-tauri/target/release/awaw.exe" "src-tauri/binaries/awaw-${TARGET_TRIPLE}.exe"
          else
            cp "src-tauri/target/release/awaw" "src-tauri/binaries/awaw-${TARGET_TRIPLE}"
          fi

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: "awawapp ${{ github.ref_name }}"
          releaseBody: |
            See the assets below to download for your platform.
          releaseDraft: true
          prerelease: false

      - name: Repackage DMG with quarantine fix and README
        if: matrix.platform == 'macos-latest'
        run: bash scripts/package-dmg.sh

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact-name }}
          path: |
            src-tauri/target/release/bundle/deb/*.deb
            src-tauri/target/release/bundle/rpm/*.rpm
            src-tauri/target/release/bundle/appimage/*.AppImage
            src-tauri/target/release/bundle/dmg/*.dmg
            src-tauri/target/release/bundle/macos/*.app.tar.gz
            src-tauri/target/release/bundle/msi/*.msi
            src-tauri/target/release/bundle/nsis/*.exe
          if-no-files-found: ignore
          retention-days: 7
